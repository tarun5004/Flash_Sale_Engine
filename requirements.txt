# Flash Sale Engine - Requirements
# Install: pip install -r requirements.txt

# Web Framework
fastapi
uvicorn[standard]

# Database (Async)
sqlalchemy[asyncio]
aiosqlite           # SQLite ke liye (development)
asyncpg             # PostgreSQL ke liye (production)
alembic             # Migrations
psycopg2-binary     # PostgreSQL driver

# Config & Security
python-dotenv
pydantic
pydantic-settings   # Pydantic v2 mein BaseSettings alag hai
email-validator     # EmailStr validation ke liye
passlib[bcrypt]     # Password hashing
python-jose         # JWT tokens

# Cache & Worker
redis
celery

# Testing
pytest
pytest-asyncio
